timeout: 0
default_entry: 4

${DEFAULT_CMDLINE}=mirror=true

/Nekonix v1.0.0-alpha
    protocol: limine
    path: boot():/boot/kernel.elf
    cmdline: debug=false loglevel=2 ${DEFAULT_CMDLINE}

/Nekonix v1.0.0-alpha (Silent)
    protocol: limine
    path: boot():/boot/kernel.elf
    cmdline: debug=false loglevel=1 ${DEFAULT_CMDLINE}
    
/Nekonix v1.0.0-alpha (Debug)
    protocol: limine
    path: boot():/boot/kernel.elf
    cmdline: debug=true loglevel=3 ${DEFAULT_CMDLINE}

/Nekonix v1.0.0-alpha (Extra Debug)
    protocol: limine
    path: boot():/boot/kernel.elf
    cmdline: debug=true loglevel=4 ${DEFAULT_CMDLINE}
